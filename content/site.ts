/**
 * Your dashboard's personalization. Edit this one file after forking.
 *
 * name → shown in the greeting ("Good morning, <name>"). Leave '' for a generic
 *        greeting. No backend, no settings screen — just this line.
 */
export const site = {
  name: '',
}
